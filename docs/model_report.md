# Model Report

## Цель

Модель оценивает вероятность победы выбранного игрока в одиночном матче РТТ на основе информации, которая была доступна до даты матча. Проект ориентирован на матчи женского одиночного разряда и использует публичные данные РТТ: календарь турниров, страницы матчей и историю рейтингов.

Отчет соответствует последнему сохраненному прогону обучения:

```text
assembled_predictor/model_results/run_20260619_235259
```

## Данные

Финальный датасет для обучения хранится в `assembled_predictor/predictor_model_dataset_from_parsers.xlsx`. Он собирается автоматически из данных парсеров и содержит двойное представление каждого матча: одна строка для перспективы первого игрока и одна строка для перспективы второго игрока.

Ключевые характеристики последнего прогона:

| Метрика | Значение |
| --- | ---: |
| Строк в исходном ml_dataset | 15764 |
| Уникальных матчей | 7882 |
| Строк в long-format | 15764 |
| Строк в feature dataset | 15764 |
| Период матчей | 2025-03-02 - 2026-06-19 |
| Матчей в train | 6996 |
| Матчей в test | 886 |
| Уникальных игроков | 1324 |
| Уникальных турниров | 154 |
| Доля побед player1 | 51.1% |
| Строк с adjusted rating diff | 15420 |
| Строк с relative rating diff | 15420 |
| Покрытие РНИ player1 / player2 | 100.0% / 100.0% |
| Покрытие рейтингов player1 / player2 | 98.8% / 99.0% |
| Используемых признаков финальной модели | 43 |

## Разделение Train/Test

Разделение выполняется по времени. Test-выборка - это последние 3 месяца от максимальной даты матча в актуальном датасете. Такой подход лучше соответствует реальному сценарию: модель обучается на прошлом и проверяется на более поздних матчах.

В последнем прогоне:

| Часть | Матчей |
| --- | ---: |
| Train | 6996 |
| Test | 886 |

Внутри train дополнительно используется validation-часть для подбора параметров CatBoost, GBM и Random Forest, а также для калибровки вероятностей CatBoost.

## Признаки

Финальная модель использует 43 признака. Основные группы:

- официальный рейтинг РТТ и рейтинговые очки до матча;
- скорректированный рейтинг: очки на один зачетный турнир;
- относительные разницы рейтинговых показателей;
- ELO до матча и ожидаемая вероятность победы по ELO;
- личные встречи игроков до даты матча;
- общая история игрока, опыт и недавняя форма;
- отдых с последнего матча;
- сила недавних соперников;
- результаты против общих соперников;
- флаги пропусков рейтинговых данных.

Исторические признаки считаются строго хронологически: матч обновляет историю только после того, как признаки для него уже рассчитаны. Это снижает риск утечки будущей информации в обучение и прогноз.

## Сравнение Моделей

Для сравнения использовалась match-level оценка с симметризацией вероятности пары игроков. Основной критерий - LogLoss на test-выборке: чем он ниже, тем лучше калибрована вероятностная оценка. Дополнительно анализируются Brier score, ROC-AUC и Accuracy. Параметры CatBoost, GBM и Random Forest подбираются на внутренней time-based validation-части train-периода, без перемешивания временных данных и без использования final test. Для CatBoost поверх базовой модели применяется sigmoid/Platt-калибровка вероятностей, обученная на внутренней validation-части train.

| Модель | N | LogLoss | Brier | ROC-AUC | Accuracy |
| --- | ---: | ---: | ---: | ---: | ---: |
| CatBoost | 886 | 0.4340 | 0.1431 | 0.8770 | 0.7935 |
| Gradient Boosting | 886 | 0.4379 | 0.1441 | 0.8759 | 0.7923 |
| Random Forest | 886 | 0.4384 | 0.1441 | 0.8747 | 0.7912 |
| Rating + ELO baseline | 886 | 0.4756 | 0.1563 | 0.8539 | 0.7652 |
| Linear Regression full features | 886 | 0.4581 | 0.1467 | 0.8715 | 0.7788 |
| Adjusted rating only | 880 | 0.5523 | 0.1760 | 0.8222 | 0.7648 |
| Relative rating only | 880 | 0.5362 | 0.1701 | 0.8386 | 0.7489 |
| ELO only | 886 | 0.5922 | 0.2032 | 0.7538 | 0.6874 |
| Rating only | 880 | 0.6815 | 0.1817 | 0.8130 | 0.7489 |

Лучший результат по основному критерию показал `CatBoostClassifier`. Он выбран как production-модель последнего прогона и сохранен в prediction bundle как `production_catboost`. Linear Regression используется как дополнительный линейный бенчмарк на полном наборе признаков и не входит в набор production-кандидатов.

## Диагностика Основных Моделей

Train/test диагностика показывает не только качество на test, но и риск переобучения. Accuracy не участвует в автоматической сортировке, но помогает интерпретировать долю верных выборов победителя при пороге 0.5.

| Модель | Train LogLoss | Test LogLoss | Gap | Train ROC-AUC | Test ROC-AUC | Train Brier | Test Brier |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| CatBoost | 0.4211 | 0.4363 | 0.0152 | 0.8907 | 0.8764 | 0.1356 | 0.1440 |
| Gradient Boosting | 0.4197 | 0.4408 | 0.0212 | 0.8910 | 0.8754 | 0.1349 | 0.1452 |
| Random Forest | 0.3722 | 0.4394 | 0.0672 | 0.9255 | 0.8751 | 0.1163 | 0.1444 |
| Linear Regression full features | 0.5113 | 0.4633 | -0.0480 | 0.8502 | 0.8712 | 0.1611 | 0.1472 |

Random Forest после подбора параметров стал заметно менее переобученным, чем в фиксированной конфигурации, но его train/test gap все еще выше, чем у CatBoost и Gradient Boosting. Gradient Boosting остается близким конкурентом, однако CatBoost дает лучший test LogLoss среди production-кандидатов и немного лучший ROC-AUC на актуальном test split. Linear Regression уступает по LogLoss, поэтому рассматривается только как диагностический бенчмарк.

## Подбор Параметров CatBoost

Для CatBoost используется тот же принцип, что и для остальных основных моделей: time-based validation search внутри train-периода. В тяжелом режиме пайплайна перебираются комбинации `iterations`, `learning_rate`, `depth`, `l2_leaf_reg` и `od_wait`; final test остается полностью отложенным.

В актуальном сохраненном наборе параметров используются следующие настройки CatBoost:

| Параметр | Значение |
| --- | ---: |
| iterations | 275 |
| learning_rate | 0.045 |
| depth | 7 |
| l2_leaf_reg | 8.0 |
| od_wait | 200 |

Полная таблица перебора в тяжелом режиме сохраняется в `catboost_hyperparameter_search.csv`.

## Калибровка Вероятностей CatBoost

После выбора параметров CatBoost поверх базовой модели обучается sigmoid/Platt-калибратор. Он получает на вход raw-вероятность CatBoost, переведенную в logit, и корректирует итоговую вероятность победы. Калибратор обучается только на внутренней validation-части train-периода; final test не используется.

| Метод | Selected | Valid LogLoss | Valid Brier | Valid ROC-AUC | Accuracy |
| --- | ---: | ---: | ---: | ---: | ---: |
| sigmoid/Platt | True | 0.4374 | 0.1419 | 0.8782 | 0.7936 |
| none | False | 0.4375 | 0.1419 | 0.8782 | 0.7957 |

## Подбор Параметров GBM И Random Forest

Для GBM и Random Forest используется не классический `GridSearchCV` с перемешиванием, а time-based validation search. Train-период дополнительно делится по времени: более ранняя часть используется для обучения кандидатов, более поздняя часть - для выбора параметров. Final test остается полностью отложенным и применяется только для итогового сравнения выбранных моделей.

В последнем прогоне выбраны следующие параметры:

| Модель | Выбранные параметры | Validation LogLoss |
| --- | --- | ---: |
| Gradient Boosting | `n_estimators=350`, `learning_rate=0.025`, `max_depth=4`, `min_samples_leaf=5`, `subsample=0.85` | 0.4317 |
| Random Forest | `n_estimators=1000`, `max_depth=12`, `min_samples_leaf=8`, `max_features=sqrt`, `class_weight=balanced_subsample` | 0.4411 |

Для GBM сетка проверяется через staged-prediction: модель обучается на максимальном числе деревьев для каждой комбинации остальных параметров, а промежуточные значения `n_estimators` оцениваются без повторного полного обучения. Для Random Forest сначала сравниваются кандидаты на 500 деревьях, затем несколько лучших конфигураций дообучаются на 1000 деревьях. Полные таблицы перебора сохраняются в `gbm_hyperparameter_search.csv` и `random_forest_hyperparameter_search.csv` внутри папки последнего прогона модели.

## Production-Модель

Алгоритм выбора production-модели детерминированный:

1. Сравниваются основные модели на test-выборке в match-level symmetrized представлении.
2. В production-кандидаты входят CatBoost, Gradient Boosting и Random Forest; простые рейтинговые модели и Linear Regression остаются бенчмарками.
3. Модели сортируются по test LogLoss по возрастанию.
4. При близком LogLoss вторым критерием выступает test Brier score по возрастанию.
5. Третьим критерием является test ROC-AUC по убыванию.
6. Accuracy и train/test LogLoss gap используются как обязательная диагностика, но не являются первичными ключами сортировки.

После выбора типа модели production-версия обучается заново на всей доступной истории матчей, чтобы пользовательский прогноз учитывал максимально свежие данные. Test-метрики при этом сохраняются отдельно и считаются только по holdout-периоду.

## Важность Признаков

Ниже приведены наиболее важные признаки для выбранной CatBoost-модели по среднему абсолютному SHAP-вкладу.

| Признак | Важность |
| --- | ---: |
| rel_diff_points_per_counting_tournament_pct_min_pre_observed_only | 0.5779 |
| expected_win_prob_elo | 0.4547 |
| elo_diff | 0.3639 |
| rel_diff_points_pct_min_pre_observed_only | 0.1825 |
| diff_rank_pre_observed_only | 0.1756 |
| diff_points_per_counting_tournament_pre_observed_only | 0.1683 |
| experience_diff | 0.0880 |
| diff_points_pre_observed_only | 0.0836 |
| opp_elo_last_5 | 0.0832 |
| elo_pre | 0.0814 |
| rest_diff_days | 0.0783 |
| player_winrate_all | 0.0731 |

Главный вклад дают рейтинговые и ELO-признаки. Дополнительные факторы - сила недавних соперников, отдых, опыт и общие соперники - помогают уточнять прогноз в пограничных матчах и улучшают калибровку вероятности.

## Пользовательский Прогноз И Панель

Для практического использования добавлена единая контрольная панель `notebooks/00_data_control_panel.ipynb`. В ней можно:

- посмотреть свежесть локальных данных;
- обновить календарь турниров РТТ;
- запустить полный пайплайн обновления данных и обучения;
- выбрать двух игроков, дату и возрастной контекст матча;
- получить прогноз вероятности победы;
- посмотреть карточки игроков на дату матча;
- изучить последние 10 матчей каждого игрока;
- посмотреть историю рейтинга, ELO, формы и вероятности победы;
- увидеть основные факторы, влияющие на прогноз;
- разобрать личные встречи и матчи против общих соперников.

## Ограничения

- Качество зависит от полноты и корректности публичных данных РТТ и текущей верстки сайта.
- Модель не учитывает травмы, покрытие, усталость вне истории матчей, поездки и другие внешние факторы.
- Метрики относятся к историческому test split и не гарантируют качество на будущих данных.
- ФИО игроков могут быть неоднозначными; часть таких случаев обрабатывается через сопоставление с РНИ и audit-таблицы.
- Проект учебный и не предназначен для ставок или коммерческого прогнозирования.
